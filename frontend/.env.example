# Opcional: URL completa da API (com /api no final).
# Em produção (Vercel) é obrigatório — não existe porta 3001 no mesmo host.
# VITE_API_BASE_URL=https://sua-api.onrender.com/api

# Login com a API (produção). Local: omita ou use false.
# VITE_AUTH_ENABLED=true
